python-pptx>=0.6.23
PyYAML>=6.0
Pillow>=10.0.0
jsonschema>=4.20.0
# cairosvg is only needed if you ingest/embed SVG assets. Splice rasterizes
# the PNG fallback through it. On macOS install Cairo first: `brew install cairo`.
cairosvg>=2.7
